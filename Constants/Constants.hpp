#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

namespace HyperEngine{

    const Vector3 gravity(0, -9.81, 0);
    const Vector3 zeroVector(0, 0, 0);
    const float dampingConst = 0.995; 
}

#endif // CONSTANTS_HPP